{
  "name": "project-steersman",
  "displayName": "Project Steersman",
  "description": "Expose VS Code's integrated browser to Claude Code via a local HTTP API and MCP server.",
  "version": "1.2.5",
  "publisher": "aarbuckle",
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.112.0"
  },
  "categories": [
    "Other"
  ],
  "extensionDependencies": [
    "ms-vscode.js-debug"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "projectSteersman",
          "title": "Project Steersman",
          "icon": "media/sidebar-icon.svg"
        }
      ]
    },
    "views": {
      "projectSteersman": [
        {
          "type": "webview",
          "id": "projectSteersman.sidebar",
          "name": "Project Steersman"
        }
      ]
    },
    "commands": [
      {
        "command": "projectSteersman.startServer",
        "title": "Start HTTP Server",
        "category": "Project Steersman"
      },
      {
        "command": "projectSteersman.stopServer",
        "title": "Stop HTTP Server",
        "category": "Project Steersman"
      },
      {
        "command": "projectSteersman.status",
        "title": "Show Status",
        "category": "Project Steersman"
      }
    ],
    "configuration": {
      "title": "Project Steersman",
      "properties": {
        "projectSteersman.httpPort": {
          "type": "number",
          "default": 3788,
          "description": "Local HTTP API port (increments if taken)."
        },
        "projectSteersman.autoStartServer": {
          "type": "boolean",
          "default": true,
          "description": "Start the HTTP server automatically when the extension activates."
        },
        "projectSteersman.startUrl": {
          "type": "string",
          "default": "about:blank",
          "description": "URL to open when the integrated browser is launched."
        },
        "projectSteersman.autoLaunchWindow": {
          "type": "boolean",
          "default": false,
          "description": "Automatically launch one integrated-browser window on activation."
        },
        "projectSteersman.autoCheckForUpdates": {
          "type": "boolean",
          "default": true,
          "description": "Periodically check GitHub for a newer version in the background."
        },
        "projectSteersman.updateCheckIntervalMinutes": {
          "type": "number",
          "default": 60,
          "minimum": 5,
          "description": "How often (in minutes) to check for updates in the background."
        },
        "projectSteersman.autoApplyUpdates": {
          "type": "boolean",
          "default": false,
          "description": "When a newer version is found, install and reload automatically instead of prompting."
        }
      }
    }
  },
  "scripts": {
    "mcp": "node mcp-server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@types/vscode": "^1.112.0"
  }
}
